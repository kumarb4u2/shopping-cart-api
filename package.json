{
  "name": "shopping-mart-services",
  "version": "1.0.0",
  "description": "Provides products to shopping-mart",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"json-server --watch db.json --port 5000\" \"node app.js\"",
    "dev": "concurrently \"json-server --watch db.json --port 5000\" \"nodemon app.js\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.27.2",
    "cors": "^2.8.5",
    "express": "^4.18.1",
    "json-server": "^0.17.0"
  },
  "devDependencies": {
    "concurrently": "^7.2.1",
    "nodemon": "^2.0.16"
  }
}
